spring:
  application:
    name: service-demo
  cloud:
    kubernetes:
      reload:
        enabled: true
        mode: polling
        period: 1000
        strategy: refresh
      config:
        namespace: config-playground
        sources:
          - name: demo-configmap
          - name: second-configmap